what is up homie
<div class="lost-group">
  <% @bball.each do |bball| %>
  <div vlass = "list-group-item">
    <ul class="ball">
      <li><%= link_to(bball.team, bball_path(bball.id))%></li>
      <li><%= link_to('DELETE', bball_path(bball.id), method: :delete)%></li>
      <li><%= link_to('EDIT', edit_bball_path(bball.id))%></li>
    </ul>
  </div>
  <% end %>
</div>